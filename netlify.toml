[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  timeout = 26

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
